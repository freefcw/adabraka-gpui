[package]
name = "fc-gpui-underscore-alias-compat"
version = "0.0.0"
edition = "2024"
publish = false

[package.metadata.gpui-macros]
crate = "fc_gpui"

[dependencies]
fc_gpui = { package = "fc-gpui", path = "../../crates/gpui-compat", default-features = false, features = ["test-support"] }
rand = "=0.9.4"
